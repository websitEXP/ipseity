

# Rename Website to "ipseity"

Simple text replacement across a few files. No design or logic changes.

## Files to Change

1. **`index.html`** — Update `<title>` to "ipseity — ons ferjani", `og:title` to "ipseity", `og:description` accordingly
2. **`src/components/header.tsx`** — Change logo link text to "ipseity"
3. **`src/components/hero-section.tsx`** — Change the `<h1>` from "somehow everything is poetic" to "ipseity"
4. **`src/pages/about.tsx`** — Update the bottom quote from "somehow everything is poetic" to "ipseity"
5. **`src/pages/contact.tsx`** — If the old name appears, update it too
6. **Project memory** — Update to reflect the new name

The subtitle quote "somehow everything is poetic" will be kept as the tagline/description where appropriate (e.g. meta description, hero subtitle) but the site name itself becomes **ipseity** everywhere.

