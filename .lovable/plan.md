

# Rename Website to "ipseity"

A simple find-and-replace across 4 files. No design, logic, or styling changes.

## Changes

1. **`index.html`** — Update `<title>` to "ipseity — ons ferjani", update `og:title` to "ipseity"
2. **`src/components/header.tsx`** — Change logo text to "ipseity"
3. **`src/components/hero-section.tsx`** — Change hero `<h1>` to "ipseity"
4. **`src/pages/about.tsx`** — Change the quote to "ipseity" (or remove the old phrase)
5. **`mem://index.md`** — Update project memory to reflect the new name

